* Ale thinks that a submenu with the list of shapes is not a good idea
* adding custom shape support ?
* should it get moved to the scrapbook as read only books? (but then it wouldn't be available in the PP); remove it from the toolbar (or leave there only the shapes which are indeed used (square and circle?); in the PP leave only the ones which are really used (square, circles, half circles?) and move the rest to the clipboards).
* Proposal for new shapes from a bug report:
Attach:notes_shapes_extensions.png
